type TypeOfInput = 'text' | 'textarea' | 'file' | 'number';

type TypeOfMediaFile = 'image' | 'video';

type ElementMovie = {
  elementList: HTMLElement;
  elementContainer: HTMLElement;
};

export { TypeOfInput, TypeOfMediaFile, ElementMovie };
